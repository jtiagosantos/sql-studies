-- para usar uma determinada base de dados
use database_name;

-- para mostrar todas as tabelas de uma base de dados
show tables;

-- para mostrar a descrição das colunas de uma tabela
describe table_name;

-- para inserir um registro na tabela
insert into table_name(fields) values(
  dados
);

-- para inserir múltiplos registros na tabela
insert into table_name(fields) values
(
  dados
),
(
  dados
),
(
  dados
);